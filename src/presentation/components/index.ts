/**
 * PRESENTATION — Components barrel
 *
 * Reusable UI components. Must be stateless or contain only local UI state.
 * Business logic must stay in hooks or use cases.
 * Future: BalanceCard, TransactionItem, BudgetBar, etc.
 */
export {};
