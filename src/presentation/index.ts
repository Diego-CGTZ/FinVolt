/**
 * PRESENTATION LAYER — index
 *
 * React Native screens, components, and UI-specific hooks.
 * Allowed imports: domain (interfaces only), application (use cases, state).
 * Must NOT import directly from: infrastructure.
 *
 * Barrel exports are added here as screens and components are built.
 */
export {};
