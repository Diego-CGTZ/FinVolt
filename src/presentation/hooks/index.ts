/**
 * PRESENTATION — Hooks barrel
 *
 * UI-specific custom hooks that consume application state/use cases.
 * These hooks are the bridge between the presentation and application layers.
 * Future: useAccounts, useTransactions, useLiquidity, useBudget.
 */
export {};
