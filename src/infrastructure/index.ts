/**
 * INFRASTRUCTURE LAYER — index
 *
 * Re-exports concrete implementations of domain interfaces.
 * This layer is allowed to import from: domain.
 * This layer must NOT import from: application, presentation.
 *
 * Barrel exports are added here as each sub-module exposes real implementations.
 */
export {};
