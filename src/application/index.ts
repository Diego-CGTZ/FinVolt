/**
 * APPLICATION LAYER — index
 *
 * Orchestrates domain logic. Allowed imports: domain.
 * Must NOT import from: infrastructure, presentation.
 * Infrastructure is injected at runtime via the DI container (src/application/di/).
 *
 * Barrel exports are added here as each sub-module exposes real use cases/state.
 */
export {};
